{{ 'sg-theme.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .sg-section-{{ section.id }}-pad {
    padding-top: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
  }
  @media screen and (min-width: 750px) {
    .sg-section-{{ section.id }}-pad {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<section class="sg-section sg-categories sg-section-{{ section.id }}-pad" style="background: {{ section.settings.bg_color }};" {{ section.shopify_attributes }}>
  {%- if section.settings.heading != blank -%}
    <div class="sg-categories__head">
      <h2 class="sg-heading">{{ section.settings.heading }}</h2>
    </div>
  {%- endif -%}

  <div class="sg-categories__scroll">
    {%- for block in section.blocks -%}
      {%- assign cat_link = block.settings.link -%}
      {%- if cat_link == blank and block.settings.collection != blank -%}
        {%- assign cat_link = block.settings.collection.url -%}
      {%- endif -%}

      <a href="{{ cat_link }}" class="sg-cat" {{ block.shopify_attributes }}>
        <div class="sg-cat__img">
          {%- if block.settings.image != blank -%}
            <img
              src="{{ block.settings.image | image_url: width: 240 }}"
              srcset="{{ block.settings.image | image_url: width: 200 }} 200w, {{ block.settings.image | image_url: width: 400 }} 400w"
              sizes="(max-width: 749px) 120px, 200px"
              alt="{{ block.settings.title | escape }}"
              width="240"
              height="240"
              loading="lazy"
            >
          {%- elsif block.settings.collection != blank and block.settings.collection.image != blank -%}
            <img
              src="{{ block.settings.collection.image | image_url: width: 240 }}"
              srcset="{{ block.settings.collection.image | image_url: width: 200 }} 200w, {{ block.settings.collection.image | image_url: width: 400 }} 400w"
              sizes="(max-width: 749px) 120px, 200px"
              alt="{{ block.settings.collection.title | default: block.settings.title | escape }}"
              width="240"
              height="240"
              loading="lazy"
            >
          {%- elsif block.settings.collection != blank and block.settings.collection.products.first.featured_image != blank -%}
            <img
              src="{{ block.settings.collection.products.first.featured_image | image_url: width: 240 }}"
              srcset="{{ block.settings.collection.products.first.featured_image | image_url: width: 200 }} 200w, {{ block.settings.collection.products.first.featured_image | image_url: width: 400 }} 400w"
              sizes="(max-width: 749px) 120px, 200px"
              alt="{{ block.settings.title | escape }}"
              width="240"
              height="240"
              loading="lazy"
            >
          {%- else -%}
            <div class="sg-placeholder">{{ block.settings.title }}</div>
          {%- endif -%}
        </div>
        <span class="sg-cat__label">{{ block.settings.title }}</span>
      </a>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "SG Categories",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop by category"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#FDFCFA"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Custom Image (overrides collection image)"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Category Name",
          "default": "Category"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Custom Link (overrides collection link)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "SG Categories",
      "blocks": [
        { "type": "category", "settings": { "title": "Party Decor" } },
        { "type": "category", "settings": { "title": "Kids Kits" } },
        { "type": "category", "settings": { "title": "DIY for Adults" } },
        { "type": "category", "settings": { "title": "Home Decor" } },
        { "type": "category", "settings": { "title": "Fairy Lights" } },
        { "type": "category", "settings": { "title": "Calendars" } },
        { "type": "category", "settings": { "title": "Pooja Decor" } },
        { "type": "category", "settings": { "title": "Notebooks" } }
      ]
    }
  ]
}
{% endschema %}
